#include <stdio.h>
int main(void)
{
	int a[2][3] = { {1,2,3},{4,5,6} };
	int(*p)[3];
	p = a;
	printf("访问a[0][1]的两种形式：\n");
	printf("用a[0][1]访问：%d\n", a[0][1]);
	printf("用（*p）[1]访问：%d\n", (*p)[1]);
	printf("访问a[1][1]的两种形式：\n");
	printf("用a[1][1]访问：%d\n", a[1][1]);
	/*printf("用(*p + 1）[1]访问：%d\n", (*p + 1)[1]);//(*p + 1)[1]访问的是a[0][2],因为p是指向元素的指针*/
	/*printf("用*（p + 1 * 3 + 1）访问:%d", *(p + 1 * 3 + 1));//error,因为p是行指针*/
	/*那用p怎么表示？？？？？？？*/
	return 0;
}